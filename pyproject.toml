[project]
name = "vla-for-robotic-arm-manipulation"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = [
    "numpy",
    "matplotlib",
    "seaborn",
    "torch",
    "pandas",
    "pandas-stubs~=3.0.3",
]
